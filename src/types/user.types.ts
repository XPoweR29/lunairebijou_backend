import { Address } from "./address.type";
import { Cart } from "./cart.types";

export interface User {
    id: string;
    name: string;
    surname: string;
    email: string;
    phoneNumber: string;
    password: string;
    
}